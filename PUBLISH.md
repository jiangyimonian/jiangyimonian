# 发布步骤

1. 在 GitHub 新建公开仓库，仓库名必须是 `jiangyimonian`。
2. 将本文件夹的内容推送到该仓库的 `main` 分支。
3. 在仓库的 `Actions` 页面分别手动运行一次：
   - `Generate contribution snake`
   - `Generate 3D contribution chart`
4. 等两个工作流完成后，刷新个人主页即可看到动态贡献图和蛇形动画。

GitHub 只有在同名公开仓库的根目录存在非空 `README.md` 时，才会将它显示在个人资料页。
